{
  "name": "belling",
  "version": "2.3.0",
  "type": "module",
  "module": "./dist/index.js",
  "types": "./index.d.ts",
  "description": "a fast declarative JavaScript library",
  "license": "MIT",
  "scripts": {
    "build": "esbuild ./index.ts --bundle --outfile=./dist/index.js --minify --sourcemap --target=es2024 --format=esm&&tsc",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chaos-cupcake/belling-js.git"
  }
}